/**
 * ffmpeg worker entrypoint — deploy separately (Fly.io / Railway / Render).
 * Consumes BullMQ merge jobs for audio & video.
 * Implemented in build step 6.
 */

console.info(
  "[mergeall-worker] Stub — start Redis + implement fluent-ffmpeg consumers in build step 6."
);
